% Arquivo CIC.sty
%
% criado em 2013 por Guilherme N. Ramos (gnramos@unb.br)
% 
% Este arquivo pode ser distribuído e/ou modificado conforme:
%     1. LaTeX Project Public License e/ou
%     2. GNU Public License.

\NeedsTeXFormat{LaTeX2e}[1999/12/01]%
\ProvidesPackage{CIC}[2013/01/01 Estilo CIC para UnBeamer.]%

% Codificação
\usepackage[brazilian]{babel}%
\usepackage[utf8]{inputenc}%

% Inicialização
\author[gnramos]{Prof. Guilherme N. Ramos\\\texttt{gnramos@unb.br}}%
\institute[CIC]{Departamento de Ciência da Computação\\Universidade de Brasília}%
\date[2014/1]{}%

\graphicspath{{img/}}%

\usepackage{CIClistings}%
\usepackage{CICflowchart}%

% Define a margem e escapechar
\lstset{xleftmargin=2\unbeamer@halfMarginWidth
,escapechar=@%  usado no comando \uncoverComment
}%

% Animação de comentários.
\newcommand{\uncoverComment}[2][+-]{\uncover<#1>{\color{greenUnB}/* #2 */}}%
% Exemplo (mostra no 2 overlay mas não no handout):
%    Escreva(MultiplicaPorY(1))  @\uncoverComment[2 | handout:0]{"10"}@

% Formatação de uma função em texto comum. Aceita argumentos opcionais entre {}s.
\def\@fnArgument#1{\texttt{(#1)}{}}%
\newcommand{\fn}[1]{\texttt{#1}\@ifnextchar\bgroup{\@fnArgument}{}}%
% Exemplos:
%    \fn{exitFailure}
%    \fn{main}{int argc, char *argv}